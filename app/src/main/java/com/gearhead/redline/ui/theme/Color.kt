package com.gearhead.redline.ui.theme

import androidx.compose.ui.graphics.Color

// Gearhead cockpit palette: near-black panels, amber primary, red "redline" accent.
val Ink = Color(0xFF0B0B0D)          // app background
val Panel = Color(0xFF15161A)        // cards / surfaces
val PanelElevated = Color(0xFF1E2026)
val Divider = Color(0xFF2A2C33)

val Amber = Color(0xFFFFB300)        // primary accent (speed, highlights)
val AmberDim = Color(0xFFB37F00)
val Redline = Color(0xFFE53935)      // top-speed / danger accent
val Ignition = Color(0xFF00E5A0)     // "go" / start accent (kept subtle)

val TextPrimary = Color(0xFFF2F3F5)
val TextSecondary = Color(0xFF9AA0AA)
val TextMuted = Color(0xFF5C616B)
