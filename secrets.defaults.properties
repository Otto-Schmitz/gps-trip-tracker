# Fallback used by the secrets-gradle-plugin when secrets.properties has no key.
# The map in the trip-detail screen renders blank tiles until a real key is set.
# To enable the map: create a file named `secrets.properties` at the project root
# with:  MAPS_API_KEY=YOUR_ANDROID_MAPS_SDK_KEY
MAPS_API_KEY=DEFAULT_MAPS_KEY_REPLACE_ME
